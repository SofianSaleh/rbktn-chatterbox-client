var Friends = {
	friends: [],

	initialize: () => {
		console.log(App.fetch.data.results[i].username);
		// $().click(Friends.toggleStatus)
	},

	toggleStatus: () => {
		// .friendStatus = true
	}

};